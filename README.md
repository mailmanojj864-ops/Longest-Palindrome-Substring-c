# Longest-Palindrome-Substring-c
Longest palindromic substring solved in c using the expand around center approach ( Leetcode#4 )
# Longest Palindromic Substring (C Implementation)

## 📌 Overview
This repository provides a clean and efficient **C implementation** to find the **Longest Palindromic Substring** in a given string.  
The solution uses the **Expand Around Center** technique, which is commonly expected in technical interviews and competitive programming.

---

## 🧠 Problem Statement
Given a string `s`, find the **longest substring** of `s` that is a palindrome.

A palindrome is a string that reads the same forward and backward.

⏱ Time Complexity
O(n²)

📦 Space Complexity
O(1) (Auxiliary Space)

